import  { StyleSheet } from 'react-native';

const styleComponent = StyleSheet.create({
    container: {
        borderColor: 'gray',
        borderWidth: 1,
        marginBottom: 10,
        padding: 10
    }
});
export {styleComponent}